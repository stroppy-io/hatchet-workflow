// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: cloud/v1/agent/logs.proto

package agentconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	agent "github.com/stroppy-io/stroppy-cloud/internal/proto/cloud/v1/agent"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AgentLogServiceName is the fully-qualified name of the AgentLogService service.
	AgentLogServiceName = "cloud.v1.agent.AgentLogService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AgentLogServiceShipLogsProcedure is the fully-qualified name of the AgentLogService's ShipLogs
	// RPC.
	AgentLogServiceShipLogsProcedure = "/cloud.v1.agent.AgentLogService/ShipLogs"
)

// AgentLogServiceClient is a client for the cloud.v1.agent.AgentLogService service.
type AgentLogServiceClient interface {
	// ShipLogs is a client stream of batches: the agent flushes LogBatch chunks
	// continuously; the server writes them to VictoriaLogs and acks counts.
	ShipLogs(context.Context) (*connect.ClientStreamForClientSimple[agent.LogBatch, agent.ShipLogsAck], error)
}

// NewAgentLogServiceClient constructs a client for the cloud.v1.agent.AgentLogService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAgentLogServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AgentLogServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	agentLogServiceMethods := agent.File_cloud_v1_agent_logs_proto.Services().ByName("AgentLogService").Methods()
	return &agentLogServiceClient{
		shipLogs: connect.NewClient[agent.LogBatch, agent.ShipLogsAck](
			httpClient,
			baseURL+AgentLogServiceShipLogsProcedure,
			connect.WithSchema(agentLogServiceMethods.ByName("ShipLogs")),
			connect.WithClientOptions(opts...),
		),
	}
}

// agentLogServiceClient implements AgentLogServiceClient.
type agentLogServiceClient struct {
	shipLogs *connect.Client[agent.LogBatch, agent.ShipLogsAck]
}

// ShipLogs calls cloud.v1.agent.AgentLogService.ShipLogs.
func (c *agentLogServiceClient) ShipLogs(ctx context.Context) (*connect.ClientStreamForClientSimple[agent.LogBatch, agent.ShipLogsAck], error) {
	return c.shipLogs.CallClientStreamSimple(ctx)
}

// AgentLogServiceHandler is an implementation of the cloud.v1.agent.AgentLogService service.
type AgentLogServiceHandler interface {
	// ShipLogs is a client stream of batches: the agent flushes LogBatch chunks
	// continuously; the server writes them to VictoriaLogs and acks counts.
	ShipLogs(context.Context, *connect.ClientStream[agent.LogBatch]) (*agent.ShipLogsAck, error)
}

// NewAgentLogServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAgentLogServiceHandler(svc AgentLogServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	agentLogServiceMethods := agent.File_cloud_v1_agent_logs_proto.Services().ByName("AgentLogService").Methods()
	agentLogServiceShipLogsHandler := connect.NewClientStreamHandlerSimple(
		AgentLogServiceShipLogsProcedure,
		svc.ShipLogs,
		connect.WithSchema(agentLogServiceMethods.ByName("ShipLogs")),
		connect.WithHandlerOptions(opts...),
	)
	return "/cloud.v1.agent.AgentLogService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AgentLogServiceShipLogsProcedure:
			agentLogServiceShipLogsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAgentLogServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAgentLogServiceHandler struct{}

func (UnimplementedAgentLogServiceHandler) ShipLogs(context.Context, *connect.ClientStream[agent.LogBatch]) (*agent.ShipLogsAck, error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("cloud.v1.agent.AgentLogService.ShipLogs is not implemented"))
}
